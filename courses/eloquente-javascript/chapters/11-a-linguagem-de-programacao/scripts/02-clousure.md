# Clousures

_Conclusão:_ Funciona porque, assim como no javascript, quando a função passada como parâmetro for executada ela terá acesso as variáveis do escopo superior, ou seja, o escopa da função que a chama.